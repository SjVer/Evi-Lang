#apply "std/io"

\? Returns twice the given value.
\? @param 0 the value to double
\? @return double the given value
@double flt (flt) ~ $0 * 2.0;

@main i32 ()
{
	puts("Hello World!");
	
	%myvar i32 double(7);
	printf("7 * 2 = %g\n", $myvar);

	~ 0;
}