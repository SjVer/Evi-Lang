{
    "tasks": [
        {
            "type": "shell",
            "label": "replace macros",
            "command": "/usr/bin/make",
            "args": [
                "replace-macros",
            ],
            "options": {
                "cwd": "${fileDirname}"
            }
        }
    ],
    "version": "2.0.0"
}